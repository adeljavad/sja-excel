# برنامه توسعه پکیج SjaExcel

## 🎯 اهداف فاز بعدی

تبدیل پروژه فعلی به یک پکیج حرفه‌ای پایتون با قابلیت‌های پیشرفته

## 📋 تسک‌های اصلی

### ۱. ساختار پکیج حرفه‌ای

#### ۱.۱ تنظیم ساختار پکیج
- [ ] ایجاد فایل `pyproject.toml` برای مدیریت وابستگی‌ها
- [ ] ایجاد فایل `setup.py` برای سازگاری با سیستم‌های قدیمی
- [ ] تنظیم `__init__.py` برای اکسپورت مناسب
- [ ] ایجاد ساختار پکیج استاندارد

#### ۱.۲ مستندسازی پکیج
- [ ] نوشتن docstring کامل برای تمام کلاس‌ها و توابع
- [ ] ایجاد فایل `LICENSE` مناسب
- [ ] نوشتن `CHANGELOG.md`
- [ ] به‌روزرسانی `README.md` برای PyPI

### ۲. پیکربندی پویا با JSON/Metadata

#### ۲.۱ سیستم مدیریت ستون‌ها
- [ ] ایجاد فایل پیکربندی `column_config.json`
- [ ] تعریف ستون‌های مهم و غیرمهم به صورت پویا
- [ ] پشتیبانی از پروفایل‌های مختلف (حسابرسی، مالی، عمومی)
- [ ] امکان بارگذاری پیکربندی سفارشی

#### ۲.۲ نمونه فایل پیکربندی
```json
{
  "important_columns": [
    "تاریخ سند",
    "شرح سند", 
    "بدهکار",
    "بستانکار",
    "شماره سند"
  ],
  "low_importance_columns": [
    "تسهيلات",
    "مشخصات پرسنلي",
    "تنخواه دار"
  ],
  "audit_columns_to_remove": [
    "مانده بستانکار",
    "مانده بدهکار"
  ]
}
```

### ۳. سیستم استخراج قابل تنظیم

#### ۳.۱ مدیریت الگوهای Regex
- [ ] ایجاد سیستم بارگذاری الگوهای regex از فایل
- [ ] پشتیبانی از الگوهای پیش‌فرض و سفارشی
- [ ] سیستم امتیازدهی و اولویت‌بندی الگوها
- [ ] امکان اضافه کردن الگوهای جدید در زمان اجرا

#### ۳.۲ نمونه فایل الگوها
```json
{
  "invoice_patterns": [
    {
      "name": "صورت وضعیت فارسی",
      "pattern": "صورت وضعیت\\s*[:؛]?\\s*(\\d+)",
      "priority": 1,
      "language": "fa"
    },
    {
      "name": "شماره انگلیسی", 
      "pattern": "Invoice\\s*#?\\s*(\\d+)",
      "priority": 2,
      "language": "en"
    }
  ],
  "currency_patterns": [
    {
      "name": "یورو با نرخ",
      "pattern": "(\\d[\\d،,\\.\\/]*)\\s*(یورو)\\s*(?:به نرخ|با نرخ)\\s*(\\d[\\d،,\\.]*)",
      "priority": 1
    }
  ]
}
```

### ۴. خروجی‌های چندگانه

#### ۴.۱ پشتیبانی از فرمت‌های مختلف
- [ ] خروجی DataFrame (پانداس)
- [ ] خروجی JSON
- [ ] خروجی CSV
- [ ] خروجی Excel (موجود)
- [ ] خروجی در حافظه (in-memory)

#### ۴.۲ API یکپارچه
```python
# خروجی‌های مختلف
df = processor.process_to_dataframe("file.xlsx")
json_data = processor.process_to_json("file.xlsx")  
csv_file = processor.process_to_csv("file.xlsx")
excel_file = processor.process_to_excel("file.xlsx")
```

### ۵. انتشار در PyPI و GitHub

#### ۵.۱ آماده‌سازی برای PyPI
- [ ] انتخاب نام مناسب (`sja-excel`)
- [ ] نوشتن توضیحات کامل برای PyPI
- [ ] تنظیم کلمات کلیدی
- [ ] ایجاد بیلد اتوماتیک

#### ۵.۲ انتشار در GitHub
- [ ] ایجاد ریپازیتوری عمومی
- [ ] تنظیم GitHub Actions برای تست اتوماتیک
- [ ] ایجاد ISSUE_TEMPLATE و PULL_REQUEST_TEMPLATE
- [ ] تنظیم GitHub Pages برای مستندات

### ۶. رابط کاربری بهبود یافته

#### ۶.۱ CLI پیشرفته
```bash
# نصب پکیج
pip install sja-excel

# استفاده
sja-excel combine file.xlsx --config audit.json --output dataframe
sja-excel extract file.xlsx --patterns custom_patterns.json
sja-excel process file.xlsx --profile financial --format json
```

#### ۶.۲ API پایتون
```python
import sja_excel as sja

# استفاده ساده
processor = sja.ExcelProcessor()
result = processor.process("file.xlsx")

# استفاده پیشرفته
processor = sja.ExcelProcessor(
    config_path="audit_config.json",
    patterns_path="custom_patterns.json"
)
df = processor.process_to_dataframe("file.xlsx")
```

## 🛠️ فنی‌سازی

### وابستگی‌ها
```toml
[project]
dependencies = [
    "pandas>=1.5.0",
    "openpyxl>=3.0.0",
    "click>=8.0.0",  # برای CLI
    "rich>=10.0.0",  # برای خروجی رنگی
]
```

### ساختار پکیج پیشنهادی
```
sja_excel/
├── __init__.py
├── core/
│   ├── __init__.py
│   ├── processor.py
│   ├── config.py
│   └── patterns.py
├── cli/
│   ├── __init__.py
│   └── main.py
├── configs/
│   ├── default.json
│   ├── audit.json
│   └── financial.json
├── patterns/
│   ├── invoice_patterns.json
│   ├── currency_patterns.json
│   └── company_patterns.json
└── utils/
    ├── __init__.py
    └── file_handlers.py
```

## 📅 برنامه زمانی پیشنهادی

### فاز ۱: زیرساخت (هفته ۱)
- ساختار پکیج
- پیکربندی JSON
- تست‌های واحد

### فاز ۲: قابلیت‌ها (هفته ۲)
- سیستم الگوهای پویا
- خروجی‌های چندگانه
- CLI پیشرفته

### فاز ۳: انتشار (هفته ۳)
- مستندسازی نهایی
- انتشار در PyPI
- انتشار در GitHub

## 🎯 مزایای پکیج نهایی

1. **نصب آسان**: `pip install sja-excel`
2. **پیکربندی پویا**: تنظیم بدون نیاز به کد نویسی
3. **قابل توسعه**: افزودن الگوهای جدید به سادگی
4. **چندخروجی**: پشتیبانی از فرمت‌های مختلف
5. **مستندات کامل**: راهنمای جامع استفاده
6. **جامعه‌محور**: قابلیت مشارکت در GitHub

## 🔧 تست و کیفیت

- [ ] پوشش تست ۹۰٪+
- [ ] تست بر روی سیستم‌های مختلف
- [ ] تست با فایل‌های نمونه واقعی
- [ ] تست عملکرد با فایل‌های بزرگ

این برنامه توسعه، پروژه فعلی را به یک پکیج حرفه‌ای تبدیل می‌کند که می‌تواند به راحتی در پروژه‌های مختلف استفاده شود و جامعه توسعه‌دهندگان از آن بهره‌مند شوند.
