# راهنمای استفاده از Smart Extractor

## 📋 معرفی

سیستم استخراج هوشمند اطلاعات از فایل‌های اکسل که قابلیت‌های زیر را ارائه می‌دهد:

1. **ترکیب شیت‌ها**: ترکیب تمام شیت‌های یک فایل اکسل در یک شیت واحد
2. **استخراج اطلاعات**: استخراج هوشمند اطلاعات از شرح‌ها
3. **فیلتر ستون‌ها**: حذف ستون‌های غیرضروری برای تحلیل حسابرسی
4. **پردازش یکپارچه**: اجرای تمام مراحل در یک فرمان

## 🚀 دستورات خط فرمان

### ۱. پردازش یکپارچه کامل

```bash
python simple_standalone.py input.xlsx all_integration -o "_all"
```

**مثال:**
```bash
python smart_extractor/simple_standalone.py c.xlsx all_integration -o "_all"
```

**خروجی:**
- فایل `c_all.xlsx` با تمام شیت‌های ترکیب شده و اطلاعات استخراج شده

### ۲. ترکیب شیت‌ها

```bash
python simple_standalone.py input.xlsx --combine-sheets -o "_combined"
```

**مثال:**
```bash
python smart_extractor/simple_standalone.py c.xlsx --combine-sheets -o "_combined"
```

**خروجی:**
- فایل `c_combined.xlsx` با تمام شیت‌های ترکیب شده

### ۳. استخراج اطلاعات

```bash
python simple_standalone.py input.xlsx -o "_extracted"
```

**مثال:**
```bash
python smart_extractor/simple_standalone.py c.xlsx -o "_extracted"
```

**خروجی:**
- فایل `c_extracted.xlsx` با اطلاعات استخراج شده

## 📊 پارامترهای خط فرمان

| پارامتر | توضیح | مقدار پیش‌فرض |
|---------|-------|---------------|
| `input_file` | مسیر فایل اکسل ورودی | الزامی |
| `operation` | نوع عملیات (`all_integration` برای پردازش کامل) | اختیاری |
| `-o`, `--output` | پسوند نام فایل خروجی | `_extracted` |
| `--combine-sheets` | ترکیب تمام شیت‌های فایل | پرچم |

## 🏗️ کلاس‌ها و API

### کلاس `ExcelSheetCombiner`

کلاس ترکیب کننده شیت‌های اکسل

#### متدها:

##### `combine_sheets(input_path, output_suffix="_combined")`
ترکیب تمام شیت‌های یک فایل اکسل در یک شیت واحد

**پارامترها:**
- `input_path`: مسیر فایل اکسل ورودی
- `output_suffix`: پسوند نام فایل خروجی

**مثال استفاده:**
```python
from smart_extractor.simple_standalone import ExcelSheetCombiner

combiner = ExcelSheetCombiner()
output_file = combiner.combine_sheets("c.xlsx", "_combined")
```

##### `analyze_column_completeness(df, threshold=0.1)`
تحلیل کامل بودن ستون‌ها و حذف ستون‌های خالی و تکراری

**پارامترها:**
- `df`: DataFrame برای تحلیل
- `threshold`: آستانه درصد داده‌های غیرخالی (پیش‌فرض: 0.1)

### کلاس `SimpleSmartExtractor`

کلاس استخراج کننده اطلاعات هوشمند

#### متدها:

##### `process_excel_file(input_path, output_suffix="_extracted")`
پردازش کامل فایل اکسل و استخراج اطلاعات

**پارامترها:**
- `input_path`: مسیر فایل اکسل ورودی
- `output_suffix`: پسوند نام فایل خروجی

**مثال استفاده:**
```python
from smart_extractor.simple_standalone import SimpleSmartExtractor

extractor = SimpleSmartExtractor()
output_file = extractor.process_excel_file("c.xlsx", "_extracted")
```

##### `extract_invoice_number(text)`
استخراج شماره صورت‌وضعیت از متن

##### `extract_currency_info(text)`
استخراج اطلاعات ارز از متن

##### `extract_company(text)`
استخراج نام شرکت از متن

##### `detect_document_type(text)`
تشخیص نوع سند از متن

## 🔧 قابلیت‌های استخراج اطلاعات

### ۱. استخراج شماره صورت‌وضعیت
- شناسایی الگوهای مختلف شماره‌گذاری
- پشتیبانی از فرمت‌های فارسی و انگلیسی
- استخراج از شرح سند و شرح ردیف سند

### ۲. استخراج اطلاعات ارز
- شناسایی مبلغ ارزی
- تشخیص نوع ارز (یورو، دلار، ریال)
- استخراج نرخ تبدیل
- پردازش فرمت‌های عددی فارسی و انگلیسی

### ۳. شناسایی شرکت‌ها
- شناسایی نام شرکت بعد از کلمه "شرکت"
- پشتیبانی از شرکت‌های شناخته شده
- استخراج از متن شرح

### ۴. تشخیص نوع سند
- تسعیر ارز
- صورت وضعیت
- چک
- انتقال
- سند متفرقه

## 🎯 فیلتر ستون‌های هوشمند

### ستون‌های مهم (همیشه حفظ می‌شوند)
- تاریخ سند
- شرح سند
- بدهکار / بستانکار
- شماره سند
- کد حساب
- پروژه
- پیمانکار/کارفرما

### ستون‌های حذف شده
- ستون‌های با کمتر از ۱۰٪ داده
- ستون‌های تکراری
- ستون‌های غیرضروری برای حسابرسی

### ستون‌های حذف شده اجباری
- مانده بستانکار
- مانده بدهکار
- تایید شده در سامانه مودیان
- چاپ سند

## 📈 نمونه خروجی

### فایل ترکیب شده
- ۳۶۳ رکورد از ۱۶ شیت مختلف
- ۱۷ ستون بهینه شده
- ستون "نام_شیت" برای ردیابی منبع داده

### فایل استخراج شده
- ۲۲ ستون شامل داده‌های استخراج شده
- شماره‌های وضعیت استخراج شده
- اطلاعات ارز استخراج شده
- شرکت‌های شناسایی شده
- نوع سند تشخیص داده شده

## 🛠️ عیب‌یابی

### خطاهای رایج

#### "فایل یافت نشد"
- بررسی مسیر فایل ورودی
- اطمینان از وجود فایل

#### "ستون description یافت نشد"
- سیستم به صورت خودکار ستون‌های شرح را پیدا می‌کند
- در صورت نیاز، ستون‌ها به صورت دستی تنظیم می‌شوند

#### "ستون‌های تکراری"
- سیستم به صورت خودکار ستون‌های تکراری را حذف می‌کند

## 📝 نکات مهم

1. **فرمت فایل**: فقط فایل‌های xlsx پشتیبانی می‌شوند
2. **کدگذاری**: پشتیبانی کامل از کاراکترهای فارسی
3. **حافظه**: برای فایل‌های بسیار بزرگ ممکن است نیاز به بهینه‌سازی باشد
4. **ستون‌ها**: سیستم به صورت هوشمند ستون‌ها را تشخیص می‌دهد

## 🔄 گردش کار پیشنهادی

1. **تست اولیه**: با فایل کوچک شروع کنید
2. **ترکیب شیت‌ها**: اگر فایل چند شیتی است
3. **استخراج اطلاعات**: برای تحلیل‌های پیشرفته
4. **پردازش یکپارچه**: برای کارهای روزمره

## 📞 پشتیبانی

برای گزارش مشکلات یا پیشنهادات:
- بررسی خطاهای کنسول
- بررسی فایل‌های خروجی
- تست با فایل‌های نمونه

---

**توسعه داده شده برای تحلیل‌های حسابرسی و مالی**
