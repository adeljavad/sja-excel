# پروژه ترکیب کننده شیت‌های اکسل - گزارش پیشرفت

## تاریخچه پروژه

**تاریخ**: ۱۱ نوامبر ۲۰۲۵  
**هدف اصلی**: ایجاد یک ابزار برای ترکیب تمام شیت‌های یک فایل اکسل در یک شیت واحد

---
Unnamed: 0	Unnamed: 1	Unnamed: 2	Unnamed: 3	Unnamed: 4	Unnamed: 5	Unnamed: 6	Unnamed: 7	Unnamed: 8	Unnamed: 9	Unnamed: 10	Unnamed: 11	Unnamed: 12	Unnamed: 13	Unnamed: 14	Unnamed: 15	Unnamed: 16	Unnamed: 17	Unnamed: 18	Unnamed: 19	Unnamed: 20	Unnamed: 21	Unnamed: 22	Unnamed: 23	Unnamed: 24	Unnamed: 25	Unnamed: 26	Unnamed: 27	Unnamed: 28	Unnamed: 29	Unnamed: 30	Unnamed: 31	Unnamed: 32	Unnamed: 33	Unnamed: 34	Unnamed: 35	Unnamed: 36	Unnamed: 37	نام_شیت	بر اساس معین- 35-01 پيش دريافت از كارفرمايان-شرکت: Nardis - نارديس از تاریخ: 1303,10,1 تا تاریخ: 1404,8,17 پروژه: پروژه NGL خارگ پيمانكار/كارفرما: شركت پترو ساحل توسعه خليج فارس 
چاپ سند	شماره عطف	شماره سند	تاریخ سند	رديف	كد حساب	شرح رديف سند	بدهكار - ريالي	بستانكار - ريالي	نوع ارز	بدهكار - ارزي	بستانكار - ارزي	نرخ تبديل ارز	صادر کننده سند	شماره پيگيري	تاريخ پيگيري	شرح سند	پروژه	نوع فعالیت/مرکز هزینه	محل ایجاد کننده هزینه	پيمانكار/كارفرما	قرارداد فروش	شماره صورتحساب فروش	قرارداد خريد	تسهيلات	مشخصات پرسنلي	تنخواه دار	انبار	اعتبارات اسنادی	شماره مجوز پرداخت	شماره مجوز دريافت	صندوقدار	شماره چك يا رسيد	تاريخ چك	سهامدار	حساب بانكي	شماره نامه اعلاميه	تاييد شده در سامانه مودیان	سپرده حسن انجام كار ارزي - اير	

تسهيلات	مشخصات پرسنلي	تنخواه دار	انبار	اعتبارات اسنادی	شماره مجوز پرداخت	شماره مجوز دريافت	صندوقدار	شماره چك يا رسيد	تاريخ چك	سهامدار	حساب بانكي	شماره نامه اعلاميه	تاييد شده در سامانه مودیان

بستانکار
بستانكار - ريالي
معادل ریالی بستانکار

بدهکار
بدهكار - ريالي
معادل ریالی بدهکار

چاپ سند	شرح رديف سند		پيمانكار/كارفرما	شرح سند	بستانكار - ريالي	رديف	شماره عطف	بستانكار - ارزي	نام_شیت	صادر کننده سند	تاریخ سند	بدهكار - ارزي	تاييد شده در سامانه مودیان	پروژه	شماره سند	نوع ارز	بدهكار - ريالي	مانده بدهکار


## تسک‌های انجام شده

### ✅ ۱. تحلیل ساختار موجود
- بررسی فایل `simple_standalone.py` موجود
- درک ساختار کلاس‌ها و توابع موجود
- شناسایی قابلیت‌های موجود برای استخراج اطلاعات

### ✅ ۲. طراحی کلاس ترکیب کننده شیت‌ها
- ایجاد کلاس `ExcelSheetCombiner`
- طراحی متد `combine_sheets()` برای ترکیب شیت‌ها
- تعریف ستون "نام_شیت" برای ردیابی منبع داده‌ها

### ✅ ۳. پیاده‌سازی قابلیت ترکیب شیت‌ها
- خواندن تمام شیت‌های یک فایل اکسل
- ترکیب داده‌ها در یک شیت واحد
- اضافه کردن ستون نام شیت برای هر ردیف

### ✅ ۴. اضافه کردن ستون نام شیت
- ستون "نام_شیت" برای نشان دادن منبع هر ردیف
- امکان ردیابی داده‌ها به شیت اصلی

### ✅ ۵. تست پیاده‌سازی
- تست با فایل `c.xlsx` شامل ۱۶ شیت
- ترکیب موفقیت‌آمیز ۳۷۹ رکورد
- ایجاد فایل خروجی با ساختار صحیح

### ✅ ۶. بهبود تشخیص سرستون‌ها و تراز ستون‌ها
- پیاده‌سازی الگوریتم تشخیص ردیف سرستون
- تراز کردن ستون‌ها در تمام شیت‌ها

### ✅ ۷. رفع مشکلات تشخیص ردیف سرستون
- بهبود الگوریتم تشخیص سرستون
- مدیریت شیت‌های با ساختارهای مختلف

### ✅ ۸. رفع خطای برچسب‌های ستون تکراری
- حل مشکل "cannot reindex on an axis with duplicate labels"
- ایجاد نام‌های ستون منحصربه‌فرد

### ✅ ۹. پیاده‌سازی مدیریت قوی ستون‌های تکراری
- ایجاد سیستم برای مدیریت ستون‌های با نام‌های تکراری
- جلوگیری از خطاهای ترکیب داده

### ✅ ۱۰. پیاده‌سازی روش ساده مبتنی بر pandas
- جایگزینی روش پیچیده تشخیص سرستون با روش ساده
- استفاده از قابلیت‌های داخلی pandas برای خواندن اکسل
- استفاده از `pd.concat()` برای ترکیب شیت‌ها به صورت ساده و کارآمد

---

## مشکلات شناسایی شده و راه‌حل‌ها

### مشکل ۱: تشخیص نادرست سرستون‌ها
**علت**: الگوریتم تشخیص سرستون پیچیده و گاهی نادرست بود
**راه‌حل**: استفاده از روش ساده با قابلیت‌های داخلی pandas

### مشکل ۲: ستون‌های تکراری
**علت**: وجود ستون‌های با نام‌های تکراری در شیت‌های مختلف
**راه‌حل**: ایجاد نام‌های منحصربه‌فرد برای ستون‌های تکراری

### مشکل ۳: تراز نادرست ستون‌ها
**علت**: ساختارهای مختلف ستون‌ها در شیت‌های مختلف
**راه‌حل**: استفاده از `pd.concat()` برای تراز خودکار ستون‌ها - ستون‌های همنام زیر هم قرار می‌گیرند و ستون‌های متفاوت با مقادیر NaN پر می‌شوند

---

## قابلیت‌های پیاده‌سازی شده

### ۱. ترکیب شیت‌ها
- خواندن تمام شیت‌های یک فایل اکسل
- ترکیب داده‌ها در یک شیت واحد
- حفظ ساختار ستون‌ها

### ۲. ردیابی منبع داده
- ستون "نام_شیت" برای هر ردیف
- امکان شناسایی شیت اصلی هر داده

### ۳. رابط خط فرمان
- پشتیبانی از آرگومان‌های خط فرمان
- امکان تعیین پسوند نام فایل خروجی
- پرچم `--combine-sheets` برای فعال‌سازی ترکیب

### ۴. گزارش‌دهی
- نمایش پیشرفت پردازش
- خلاصه نتایج
- آمار تعداد رکوردها و ستون‌ها

---

## دستورات قابل استفاده

```bash
# ترکیب شیت‌های یک فایل اکسل
python smart_extractor/simple_standalone.py input_file.xlsx --combine-sheets

# ترکیب با پسوند سفارشی
python smart_extractor/simple_standalone.py input_file.xlsx --combine-sheets -o "_combined"

# استخراج اطلاعات (عملکرد اصلی قدیم)
python smart_extractor/simple_standalone.py input_file.xlsx
```

---

## تست‌های انجام شده

### تست با فایل `c.xlsx`
- **تعداد شیت‌ها**: ۱۶
- **رکوردهای ترکیب شده**: ۳۷۹
- **ستون‌های خروجی**: ۴۰ (شامل ستون نام شیت)
- **نتیجه**: موفقیت‌آمیز

### تست با پسوند سفارشی
- **پسوند**: `_combined`
- **فایل خروجی**: `c_combined.xlsx`
- **نتیجه**: موفقیت‌آمیز

### تست با روش ساده pd.concat()
- **پسوند**: `_simple`
- **فایل خروجی**: `c_simple.xlsx`
- **رکوردها**: ۳۷۹
- **ستون‌ها**: ۴۰
- **نتیجه**: موفقیت‌آمیز

### تست با روش نهایی قابل اعتماد
- **پسوند**: `_final`
- **فایل خروجی**: `c_final.xlsx`
- **رکوردها**: ۳۶۳
- **ستون‌ها**: ۴۹ (بدون ستون‌های Unnamed)
- **نتیجه**: موفقیت‌آمیز - ستون‌های همنام زیر هم قرار گرفتند

### تست با فیلتر ستون‌های خالی
- **پسوند**: `_filtered`
- **فایل خروجی**: `c_filtered.xlsx`
- **رکوردها**: ۳۶۳
- **ستون‌ها**: ۲۷ (کاهش ۲۲ ستون خالی)
- **نتیجه**: موفقیت‌آمیز - ستون‌های کم اهمیت حذف شدند

### تست با حذف ستون‌های تکراری
- **پسوند**: `_deduplicated`
- **فایل خروجی**: `c_deduplicated.xlsx`
- **رکوردها**: ۳۶۳
- **ستون‌ها**: ۲۱ (کاهش ۶ ستون تکراری)
- **نتیجه**: موفقیت‌آمیز - ستون‌های یکسان ادغام شدند

### تست نهایی با گروه‌های تکراری اضافی
- **پسوند**: `_final`
- **فایل خروجی**: `c_final_1.xlsx`
- **رکوردها**: ۳۶۳
- **ستون‌ها**: ۲۱ (حفظ ستون‌های با داده کافی)
- **نتیجه**: موفقیت‌آمیز - ستون‌های ارزی نیز ادغام شدند

### تست نهایی بهینه‌سازی شده برای تحلیل حسابرسی
- **پسوند**: `_clean`
- **فایل خروجی**: `c_clean.xlsx`
- **رکوردها**: ۳۶۳
- **ستون‌ها**: ۱۷ (حذف ستون‌های غیرضروری برای حسابرسی)
- **نتیجه**: موفقیت‌آمیز - ستون‌های "مانده بستانکار"، "مانده بدهکار"، "تاييد شده در سامانه مودیان"، "چاپ سند" حذف شدند

---

## تسک‌های باقی مانده

### ✅ ۱. فیلتر ستون‌های خالی
- تحلیل کامل بودن ستون‌ها
- حذف ستون‌های با کمتر از ۱۰٪ داده
- حفظ ستون‌های مهم مانند "تاریخ سند"، "شرح سند"، "بدهکار"، "بستانکار"
- کاهش ۲۲ ستون از ۴۹ به ۲۷ ستون

### ✅ ۲. حذف ستون‌های تکراری
- شناسایی ستون‌های یکسان مانند "بستانکار = بستانكار - ريالي = معادل ریالی بستانکار"
- شناسایی ستون‌های ارزی تکراری مانند "بستانكار - ارزي = بستانکار ارزی"
- نگهداری فقط یک نسخه از هر گروه ستون تکراری
- کاهش ۶ ستون دیگر از ۲۷ به ۲۱ ستون
- مجموع کاهش: ۲۸ ستون از ۴۹ به ۲۱ ستون (۵۷٪ کاهش)

### ✅ ۳. حذف ستون‌های غیرضروری برای تحلیل حسابرسی
- حذف اجباری ستون‌های "مانده بستانکار"، "مانده بدهکار"
- حذف اجباری ستون‌های "تاييد شده در سامانه مودیان"، "چاپ سند"
- کاهش ۴ ستون دیگر از ۲۱ به ۱۷ ستون
- مجموع کاهش نهایی: ۳۲ ستون از ۴۹ به ۱۷ ستون (۶۵٪ کاهش)

### 🔄 ۲. بهبود مدیریت خطا
- مدیریت بهتر خطاهای خواندن شیت‌های خاص
- گزارش‌دهی دقیق‌تر خطاها

### 🔄 ۳. پشتیبانی از چندین فایل اکسل
- امکان ترکیب شیت‌ها از چندین فایل مختلف
- مدیریت نام‌های تکراری در فایل‌های مختلف

### 🔄 ۴. قابلیت‌های پیشرفته فیلترینگ
- فیلتر کردن داده‌ها قبل از ترکیب
- انتخاب ستون‌های خاص برای ترکیب

### 🔄 ۵. بهینه‌سازی عملکرد
- مدیریت فایل‌های بسیار بزرگ
- استفاده از حافظه بهینه

### 🔄 ۶. مستندسازی کامل
- مستندسازی API
- مثال‌های استفاده
- راهنمای عیب‌یابی

### 🔄 ۷. تست‌های واحد
- ایجاد تست‌های واحد برای تمام قابلیت‌ها
- تست با فایل‌های نمونه مختلف

---

## ساختار فایل‌های ایجاد شده

```
smart_extractor/
├── simple_standalone.py          # فایل اصلی با قابلیت ترکیب شیت‌ها
├── project_progress_report.md    # این فایل - گزارش پیشرفت
├── c.xlsx                        # فایل تست اصلی
├── c_extracted.xlsx              # فایل خروجی تست ۱
├── c_extracted_1.xlsx            # فایل خروجی تست ۲  
├── c_extracted_2.xlsx            # فایل خروجی تست ۳
└── c_combined.xlsx               # فایل خروجی با پسوند سفارشی
```

---

## نکات فنی مهم

### معماری
- استفاده از الگوی کلاس‌محور
- جداسازی منطق ترکیب از منطق استخراج
- قابلیت استفاده مستقل از کلاس‌ها
- استفاده از `pd.concat()` برای ترکیب ساده و کارآمد
- سیستم فیلتر ستون‌های خالی

### وابستگی‌ها
- pandas برای مدیریت داده‌ها
- pathlib برای مدیریت مسیرها
- argparse برای رابط خط فرمان

### راه‌حل نهایی قابل اعتماد با فیلتر ستون‌ها
```python
# خواندن شیت بدون فرض سرستون
df_raw = pd.read_excel(input_path, sheet_name=sheet_name, header=None)

# پیدا کردن ردیف سرستون با کلمات کلیدی
header_row = 0
for i in range(min(3, len(df_raw))):
    row_text = df_raw.iloc[i].astype(str).str.lower()
    header_keywords = ['شماره', 'تاریخ', 'سند', 'حساب', 'شرح', 'بدهکار', 'بستانکار']
    header_count = sum(any(keyword in cell for keyword in header_keywords) for cell in row_text)
    
    if header_count >= 2:
        header_row = i
        break

# خواندن شیت با سرستون صحیح
df = pd.read_excel(input_path, sheet_name=sheet_name, header=header_row)

# حذف ستون‌های Unnamed
df = df.loc[:, ~df.columns.str.contains('^Unnamed')]

# اضافه کردن ستون نام شیت
df["نام_شیت"] = sheet_name

# ترکیب با pd.concat()
combined_df = pd.concat(all_data, axis=0, ignore_index=True, sort=False)

# تحلیل و حذف ستون‌های خالی
combined_df = self.analyze_column_completeness(combined_df, threshold=0.1)

# ذخیره در اکسل
combined_df.to_excel(output_filename, index=False)
```

### سیستم فیلتر ستون‌ها
```python
def analyze_column_completeness(self, df, threshold=0.1):
    """تحلیل کامل بودن ستون‌ها و حذف ستون‌های خالی"""
    total_rows = len(df)
    if total_rows == 0:
        return df
    
    columns_to_keep = []
    columns_to_remove = []
    
    for column in df.columns:
        if column == self.sheet_name_column:
            columns_to_keep.append(column)
            continue
            
        # ستون‌های مهم همیشه حفظ شوند
        if any(important_col in str(column) for important_col in self.important_columns):
            columns_to_keep.append(column)
            continue
        
        # محاسبه درصد داده‌های غیرخالی
        non_empty_count = df[column].notna().sum()
        completeness_ratio = non_empty_count / total_rows
        
        # اگر ستون کم اهمیت است و کمتر از آستانه داده دارد، حذف شود
        if any(low_col in str(column) for low_col in self.low_importance_columns):
            if completeness_ratio < threshold:
                columns_to_remove.append(column)
            else:
                columns_to_keep.append(column)
        else:
            # ستون‌های دیگر اگر کمتر از آستانه داده دارند حذف شوند
            if completeness_ratio < threshold:
                columns_to_remove.append(column)
            else:
                columns_to_keep.append(column)
    
    # حذف ستون‌های تکراری
    columns_to_keep = list(set(columns_to_keep))
    
    print(f"   📊 تحلیل ستون‌ها: {len(columns_to_keep)} ستون نگهداری شد، {len(columns_to_remove)} ستون حذف شد")
    if columns_to_remove:
        print(f"   🗑️ ستون‌های حذف شده: {', '.join(columns_to_remove[:5])}{'...' if len(columns_to_remove) > 5 else ''}")
    
    return df[columns_to_keep]
```

### محدودیت‌ها
- نیاز به pandas نصب شده
- مدیریت حافظه برای فایل‌های بسیار بزرگ
- پشتیبانی از فرمت‌های xlsx فقط

---

## گام بعدی

برای ادامه کار فردا، می‌توانیم بر روی موارد زیر تمرکز کنیم:

1. **پیاده‌سازی ترکیب چند فایل اکسل**
2. **افزودن قابلیت فیلترینگ پیشرفته**
3. **ایجاد تست‌های واحد جامع**
4. **بهینه‌سازی عملکرد برای فایل‌های بزرگ**

این گزارش تمام کارهای انجام شده و برنامه‌های آینده را مستند کرده است.
