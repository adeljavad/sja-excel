# راهنمای تحلیل هوشمند شیت‌ها

## 📋 مقدمه

این راهنما نحوه استفاده از اسکریپت تحلیل هوشمند شیت‌ها را توضیح می‌دهد. این اسکریپت به صورت هوشمند شیت‌های دو فایل اکسل را بر اساس مبالغ تطبیق می‌دهد و گزارش تحلیلی ایجاد می‌کند.

## 🎯 قابلیت‌های اصلی

- **شناسایی هوشمند ستون‌های مبلغی** (بدهکار/بستانکار)
- **تبدیل خودکار مقادیر به عدد**
- **گروه‌بندی داده‌ها بر اساس نام شیت**
- **تحلیل تطبیق بر اساس مبالغ**
- **ایجاد گزارش‌های تحلیلی کامل**

## 📁 ساختار فایل‌ها

### فایل‌های ورودی
- **c_e.xlsx** - فایل شرکت A
- **d_e.xlsx** - فایل شرکت B

### فایل‌های خروجی
- **sheet_analysis_report.xlsx** - گزارش تحلیل پیشرفته

## 🚀 دستور اجرایی

### تحلیل پیشرفته بر اساس نام شیت‌ها

```bash
python sheet_analysis.py c_e.xlsx d_e.xlsx -o sheet_analysis_report.xlsx
```

### تحلیل هوشمند بر اساس مبالغ

```bash
python smart_sheet_analysis.py c_e.xlsx d_e.xlsx -o smart_analysis_report.xlsx
```

## 📊 خروجی‌های سیستم

### 1. گزارش تحلیل پیشرفته (`sheet_analysis_report.xlsx`)

این گزارش شامل شیت‌های زیر است:

#### 🔹 مقایسه_شیت‌ها
- نام شیت‌های تطبیق شده
- درصد تشابه نام
- مبالغ بدهکار و بستانکار
- درصد تطابق مبالغ
- نوع حساب
- وضعیت تطابق

#### 🔹 خلاصه_فایل_A
- لیست تمام شیت‌های فایل A
- تعداد رکوردهای هر شیت
- جمع مبالغ بدهکار و بستانکار
- مبلغ خالص هر شیت

#### 🔹 خلاصه_فایل_B
- لیست تمام شیت‌های فایل B
- تعداد رکوردهای هر شیت
- جمع مبالغ بدهکار و بستانکار
- مبلغ خالص هر شیت

#### 🔹 آمار_کلی
- تعداد شیت‌های هر فایل
- تعداد تطابق‌های یافت شده
- میانگین تطابق مبالغ
- بهترین و بدترین تطابق‌ها

### 2. گزارش تحلیل هوشمند (`smart_analysis_report.xlsx`)

این گزارش شامل شیت‌های زیر است:

#### 🔹 خلاصه_فایل_A
- خلاصه شیت‌های فایل A با مبالغ

#### 🔹 خلاصه_فایل_B
- خلاصه شیت‌های فایل B با مبالغ

#### 🔹 تطابق‌ها
- شیت‌های تطبیق شده بر اساس مبالغ
- تشابه مبلغ و تشابه نام
- امتیاز کلی تطابق

#### 🔹 آمار_کلی
- آمار کلی تحلیل

## 🔍 نحوه کار سیستم

### 1. شناسایی ستون‌های مبلغی

سیستم به صورت هوشمند ستون‌های بدهکار و بستانکار را شناسایی می‌کند:

- **کلمات کلیدی بدهکار**: `بدهكار`, `دبیت`, `debit`, `بدهی`, `بدکار`
- **کلمات کلیدی بستانکار**: `بستانكار`, `کریدیت`, `credit`, `بستانکاری`, `بستکار`

### 2. تبدیل مقادیر به عدد

سیستم مقادیر متنی را به عدد تبدیل می‌کند:
- تبدیل `"125031804"` به `125031804`
- تبدیل `"0"` به `0`
- جایگزینی مقادیر نامعتبر با `0`

### 3. گروه‌بندی بر اساس شیت

داده‌ها بر اساس ستون `نام_شیت` گروه‌بندی می‌شوند:
- محاسبه تعداد رکوردها
- جمع مبالغ بدهکار
- جمع مبالغ بستانکار
- محاسبه مبلغ خالص

### 4. تحلیل تطبیق

سیستم شیت‌ها را بر اساس موارد زیر تطبیق می‌دهد:

#### 🔸 تطابق مبلغی
- تطابق مستقیم مبالغ
- تطابق بدهکار با بستانکار (مقادیر مخالف)
- تطابق مبالغ صفر

#### 🔸 تشابه نام
- محاسبه تشابه نام شیت‌ها
- استفاده از الگوریتم SequenceMatcher

#### 🔸 امتیاز کلی
- 70% وزن برای تشابه مبلغ
- 30% وزن برای تشابه نام

## 🎯 نمونه نتایج

### بهترین تطابق‌ها
1. **سپرده حسن انجام کار ارزی**: 82.4% تطابق
2. **سپرده حسن انجام کار ارزی**: 81.5% تطابق  
3. **سپرده بیمه ارزی**: 76.5% تطابق

### نیاز به بررسی
- **پیش پرداخت ارزی**: 63.2% تطابق
- **سپرده بیمه ارزی**: 66.7% تطابق
- **سپرده بیمه ارزی**: 72.2% تطابق

## ⚙️ تنظیمات پیشرفته

### تغییر حداقل تشابه

برای تغییر حداقل تشابه مبلغی (پیش‌فرض: 80%):

```python
# در فایل smart_sheet_analysis.py
if amount_similarity > 80:  # تغییر این مقدار
```

### تغییر وزن‌ها

برای تغییر وزن تشابه مبلغ و نام:

```python
# در فایل smart_sheet_analysis.py
'امتیاز_کلی': (amount_similarity * 0.7) + (name_similarity * 0.3)  # تغییر ضرایب
```

## 🛠️ عیب‌یابی

### مشکل: ستون‌های مبلغی شناسایی نمی‌شوند

**راه حل**: 
- بررسی کنید که ستون‌ها حاوی کلمات کلیدی باشند
- از اسکریپت `analyze_c_structure.py` برای بررسی ساختار استفاده کنید

### مشکل: مقادیر به درستی جمع نمی‌شوند

**راه حل**:
- مطمئن شوید مقادیر به عدد تبدیل شده‌اند
- از اسکریپت `debug_c_file.py` برای دیباگ استفاده کنید

## 📈 نکات مهم

1. **بدون مپینگ ثابت**: سیستم از مپینگ ثابت استفاده نمی‌کند
2. **تحلیل هوشمند**: تطابق بر اساس مبالغ واقعی انجام می‌شود
3. **شناسایی خودکار**: ستون‌ها به صورت خودکار شناسایی می‌شوند
4. **گزارش‌های کامل**: گزارش‌های تحلیلی با جزئیات کامل ایجاد می‌شوند

## 📞 پشتیبانی

برای سوالات و مشکلات:
1. فایل‌های ورودی را بررسی کنید
2. از اسکریپت‌های دیباگ استفاده کنید
3. گزارش خطاها را بررسی کنید

---

**تاریخ ایجاد**: 1403/08/21  
**آخرین به‌روزرسانی**: 1403/08/21
