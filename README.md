# Smart Extractor - سیستم استخراج هوشمند اطلاعات

یک سیستم ماژولار برای استخراج هوشمند اطلاعات از شرح اسناد فارسی و انگلیسی

## 🎯 ویژگی‌ها

- **استخراج شماره صورت‌وضعیت** از متن‌های فارسی و انگلیسی
- **استخراج اطلاعات ارز** (مبلغ، نرخ، نوع)
- **شناسایی شرکت‌ها** از متن
- **تشخیص نوع سند** (صورت وضعیت، چک، تسعیر ارز، انتقال)
- **طراحی ماژولار** برای استفاده در پلتفرم‌های مختلف
- **پشتیبانی از فایل‌های اکسل** با نام‌گذاری خودکار

## 🏗️ ساختار ماژول

```
smart_extractor/
├── core/                    # ماژول هسته
│   ├── __init__.py
│   ├── extractors.py       # کلاس اصلی استخراج کننده
│   ├── models.py           # مدل‌های داده
│   └── patterns.py         # الگوهای regex
├── processors/             # پردازش‌گرها
│   ├── __init__.py
│   └── excel_processor.py  # پردازش فایل‌های اکسل
├── utils/                  # ابزارهای کمکی
│   ├── __init__.py
│   └── file_handler.py     # مدیریت فایل و نام‌گذاری
├── integrations/           # آداپتورهای یکپارچه‌سازی
│   ├── __init__.py
│   ├── odoo_integration.py # یکپارچه‌سازی با اودوو
│   └── django_integration.py # یکپارچه‌سازی با جنگو
├── standalone.py           # اسکریپت مستقل
├── test_sample.py          # اسکریپت تست
└── README.md
```

## 🚀 استفاده سریع

### 1. استفاده در پایتون ساده

```python
from smart_extractor.core.extractors import SmartExtractor

# ایجاد استخراج کننده
extractor = SmartExtractor()

# استخراج از یک متن
text = "صورت وضعیت شماره 1234 - 1000 یورو با نرخ 50000"
result = extractor.extract_from_text(text)

print(f"شماره وضعیت: {result.invoice_number}")
print(f"مبلغ ارزی: {result.currency_info.amount}")
print(f"نوع ارز: {result.currency_info.currency}")
print(f"نرخ: {result.currency_info.rate}")
```

### 2. پردازش فایل اکسل

```python
from smart_extractor.processors.excel_processor import ExcelProcessor

# پردازش فایل اکسل
processor = ExcelProcessor()
output_file = processor.process_excel_file("data.xlsx")

print(f"فایل خروجی: {output_file}")
```

### 3. اسکریپت مستقل

```bash
# پردازش فایل اکسل
python smart_extractor/standalone.py data.xlsx

# با پسوند دلخواه
python smart_extractor/standalone.py data.xlsx -o "_processed"
```

## 📊 ستون‌های استخراج شده

پس از پردازش، این ستون‌ها به فایل اکسل اضافه می‌شوند:

| ستون فارسی | ستون انگلیسی | توضیح |
|------------|--------------|-------|
| شماره_وضعیت | invoice_number | شماره صورت‌وضعیت استخراج شده |
| مبلغ_ارزی | currency_amount | مبلغ ارزی استخراج شده |
| نوع_ارز | currency_type | نوع ارز (یورو، دلار، ریال) |
| نرخ_ارز | exchange_rate | نرخ تسعیر ارز |
| نام_شرکت | company_name | نام شرکت شناسایی شده |
| نوع_سند | document_type | نوع سند (صورت وضعیت، چک، ...) |
| اطمینان_استخراج | extraction_confidence | میزان اطمینان استخراج (0-1) |

## 🔧 یکپارچه‌سازی

### با اودوو

```python
from smart_extractor.integrations.odoo_integration import OdooIntegration

# ایجاد یکپارچه‌ساز
odoo_integration = OdooIntegration()

# استخراج از خطوط سند حسابداری
move_lines = [...]  # خطوط سند از اودوو
extracted_data = odoo_integration.extract_from_account_move_lines(move_lines)
```

### با جنگو

```python
from smart_extractor.integrations.django_integration import DjangoIntegration

# ایجاد یکپارچه‌ساز
django_integration = DjangoIntegration()

# استخراج از نمونه‌های مدل
instances = MyModel.objects.all()
extracted_data = django_integration.extract_from_model_instances(instances)
```

## 🧪 تست سیستم

```bash
# اجرای تست جامع
python smart_extractor/test_sample.py
```

## 📝 نمونه خروجی

```
🚀 سیستم استخراج هوشمند - نسخه مستقل
==================================================
📖 خواندن فایل: sample_data.xlsx
   📄 پردازش شیت: Sheet1
   ✅ کل رکوردها: 5
🔍 استخراج اطلاعات از ستون 'description'...
✅ 5 رکورد پردازش شد
💾 فایل خروجی ذخیره شد: sample_data_extracted.xlsx

📊 خلاصه نتایج:
   کل رکوردها: 5
   شماره‌های وضعیت استخراج شده: 3
   اطلاعات ارز استخراج شده: 2
   شرکت‌های شناسایی شده: 3
   میانگین اطمینان: 0.85
   نرخ موفقیت: 50.0%

🎉 پردازش با موفقیت تکمیل شد!
📁 فایل خروجی: sample_data_extracted.xlsx
```

## 🔍 الگوهای شناسایی

### شماره صورت‌وضعیت
- `صورت وضعیت شماره 1234`
- `شماره صورت وضعیت: 5678`
- `Invoice #999`
- `INV 1001`

### اطلاعات ارز
- `1000 یورو با نرخ 50000`
- `2000 دلار`
- `1500 EUR at rate 55000`
- `مبلغ 1,000,000 ریال`

### شرکت‌ها
- ایران، ایرایتک، پترو ساحل، فرآب، ناردیس، خارک
- Iran, Iratec, Petro Sahel, Farab, Nardis, Khark

## 📦 وابستگی‌ها

- `pandas` - پردازش داده‌ها
- `openpyxl` - خواندن/نوشتن فایل‌های اکسل

## 🎯 موارد استفاده

- **حسابداری**: استخراج اطلاعات از اسناد مالی
- **بانک‌داری**: پردازش صورت‌حساب‌ها
- **مدیریت پروژه**: استخراج اطلاعات از صورت‌وضعیت‌ها
- **داده‌کاوی**: غنی‌سازی داده‌های متنی

## 📚 مستندات کامل

برای راهنمای کامل استفاده از سیستم، مستندات زیر را مطالعه کنید:

- **[USAGE_GUIDE.md](USAGE_GUIDE.md)** - راهنمای کامل دستورات خط فرمان و API
- **[project_progress_report.md](project_progress_report.md)** - گزارش پیشرفت پروژه

### 🚀 دستورات جدید یکپارچه

```bash
# پردازش یکپارچه کامل (ترکیب شیت‌ها + استخراج اطلاعات + فیلتر ستون‌ها)
python smart_extractor/simple_standalone.py c.xlsx all_integration -o "_all"

# ترکیب شیت‌ها
python smart_extractor/simple_standalone.py c.xlsx --combine-sheets -o "_combined"

# استخراج اطلاعات
python smart_extractor/simple_standalone.py c.xlsx -o "_extracted"
```

## 📞 پشتیبانی

برای گزارش مشکلات یا پیشنهادات، لطفاً Issues در ریپازیتوری ایجاد کنید.

---

**توسعه داده شده با ❤️ برای جامعه نرم‌افزار ایران**
